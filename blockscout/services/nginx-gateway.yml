services:
  gateway:
    image: nginx
    restart: always
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    volumes:
      - "../proxy/gateway:/etc/nginx/templates"
    ports:
      - "${BASE_GATEWAY_PUBLIC_PORT:-8000}:80"
      - "8080:8080"
      - "8081:8081"
